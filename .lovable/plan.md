Remover a classe `max-w-3xl` dos dois parágrafos na seção "Pilares do evento" em `src/routes/index.tsx` (linhas ~193-206), para que ocupem toda a largura do container (mesma largura da grid dos 4 cards).